# Core data contracts and validation
pydantic>=2.7,<3

# Anthropic Claude API (Sonnet for extraction, Haiku for classification)
anthropic>=0.39

# PostgreSQL (production-grade storage, not SQLite)
SQLAlchemy>=2.0,<3
psycopg[binary]>=3.1

# PDF parsing with page/table preservation
pdfplumber>=0.11

# Mandate config + local .env loading
PyYAML>=6.0
python-dotenv>=1.0

# Query/retrieval API
fastapi>=0.110
uvicorn[standard]>=0.29

# Sample deck generation (demo data)
reportlab>=4.0

# Test + lint (dev)
pytest>=8.0
httpx>=0.27        # FastAPI TestClient
ruff>=0.5
mypy>=1.10
